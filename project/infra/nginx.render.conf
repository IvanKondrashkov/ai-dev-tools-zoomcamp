server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing - all routes should be handled by index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}

